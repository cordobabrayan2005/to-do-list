import { defineConfig } from 'vite'

export default {
  server: {
    port: 3000,   // aquí cambias el puerto (ej: 3000)
    host: "0.0.0.0" // esto permite acceder desde tu red local (ej: desde el celular)
  }
};
